{
  "routes": [
    {
      "method": "GET",
      "path": "/organizations",
      "handler": "organizations.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/organizations/count",
      "handler": "organizations.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/organizations/:id",
      "handler": "organizations.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/organizations",
      "handler": "organizations.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/organizations/:id",
      "handler": "organizations.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/organizations/:id",
      "handler": "organizations.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
